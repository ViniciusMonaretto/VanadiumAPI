# Minimal Mosquitto MQTT Broker Configuration

# Allow anonymous connections
allow_anonymous true

# Listen on default MQTT port
listener 1883

# Listen on WebSocket port for MQTT over WebSocket
listener 9001
protocol websockets

# Enable logging
log_dest stdout
log_type all
log_timestamp true

# Persistence settings
persistence true
persistence_location /mosquitto/data/
